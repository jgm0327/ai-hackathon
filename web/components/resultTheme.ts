/**
 * Figma "02 · 변환 결과" 섹션(`286:7123`)의 색 토큰 (9/18 신규).
 *
 * 이 섹션은 그 전까지 쓰던 중성 회색(`#1a1a1a`/`#242424`/`#f2f2f2`/`#828282`)이 아니라
 * **따뜻한 톤**으로 다시 그려졌다. 사용자가 적용 범위를 "이 섹션 화면만"으로 확정해서
 * (9/18), `globals.css`의 전역 토큰은 건드리지 않고 여기에만 모아 둔다 — 홈/스택/
 * 경력기술서는 기존 회색 그대로다.
 *
 * 나중에 앱 전체를 이 톤으로 통일하기로 하면, 이 파일의 값을 `globals.css`의
 * `:root`로 옮기고 각 화면이 클래스 대신 변수를 쓰게 바꾸면 된다.
 */
export const resultTheme = {
  /** 바텀시트 / 전체 화면 배경 */
  sheetBg: "#1b1b1b",
  /** 3.1-q처럼 시트가 아니라 화면 전체일 때의 배경 (시트보다 아주 약간 더 어둡다) */
  screenBg: "#1a1917",
  /** 문장 카드 · 입력창 · 제안 박스 */
  cardBg: "#2a2724",
  /** 본문 글자 */
  text: "#ede9e2",
  /** 카드 안 본문처럼 한 단계 낮춘 글자 */
  textSoft: "#bdb7ae",
  /** 라벨 · 보조 설명 */
  textMuted: "#918c84",
  /** 보조 버튼 테두리 */
  border: "#34322e",
  /** 바텀시트 grabber 손잡이 */
  handle: "#3c372f",
  /** 시트 헤더의 "저장" — 강조색(#ff5d00)보다 한 단계 밝아 글자로 읽기 좋다 */
  action: "#ff9a3d",
} as const;
