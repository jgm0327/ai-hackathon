# CLAUDE.md — 원티드 AI 해커톤: 커리어 로그

이 문서는 이 레포에서 작업하는 모든 코딩 에이전트(Claude Code, Orca worktree 인스턴스 포함)가
가장 먼저 읽어야 하는 최상위 컨텍스트다. 세부 작업 지침은 `tasks/`, 배경 설계는 `docs/`를 참고한다.

> **2026-09-13 대폭 개정.** 제품 방향과 기술 스택이 모두 바뀌었다.
> 이전 버전의 "온보딩 다이어리 / Streamlit / Streamlit Cloud"를 전제한 코드와 문서는
> 전부 이 문서 기준으로 재검토할 것. 이식 절차는 `docs/06-migration.md`.

---

## 1. 프로젝트 한 줄 요약

유저가 퇴근 직전 대충 남긴 업무 낙서("결제 터진 거 막음")를 쌓아두었다가,
**이직이 필요한 순간에 프로젝트 단위로 묶어 완성된 경력기술서로 변환해주는 서비스.**

핵심 가치는 단건 변환이 아니라 **시간차 누적**에 있다.
2월에 쓴 "레디스 붙임"과 3월에 쓴 "오류율 0.8% → 0.3%"가 하나의 STAR 문장으로 합쳐지는 것 —
이것이 제품의 유일한 결정적 순간이고, 모든 설계 판단은 이 장면을 지키는 쪽으로 내린다.

**사용 맥락 2가지** (기능 판단 시 이 둘 중 하나를 만족해야 한다)
1. 퇴근 15분 전, 자리에서 한 줄 타이핑
2. 자차 출퇴근 중, 폰으로 음성 입력

2번 때문에 모바일은 포기 불가다. 이것이 Streamlit을 걷어낸 근본 이유다.

---

## 2. 절대 원칙 (기능 판단의 기준선)

기능을 넣을지 말지 헷갈리면 이 네 개로 판단한다.

### 2.1 평소 입력 비용은 0이어야 한다
슬로건은 **"3초 만에 끝내고 모니터 끄기"**다.
매일 반복되는 경로(메모 입력 → 변환)에 선택지·드롭다운·분류 작업을 추가하지 않는다.
유저에게 무언가를 고르게 하려면 그 빈도가 **월 1회 이하**여야 한다.

### 2.2 숫자를 지어내지 않는다
경력기술서는 면접에서 검증당하는 문서다.
**유저가 기록에 쓰지 않은 수치는 어떤 경우에도 생성하지 않는다.**
숫자가 없으면 결과 항목을 비우거나, 유저에게 되묻는다(건너뛰기 가능).
이 원칙을 깨는 프롬프트는 리뷰에서 반려한다.

### 2.3 분류는 임베딩, 묶기는 LLM
- **임베딩**: 새 카드를 기존 작업 그룹에 배정 (보조적, 틀려도 손해 작음)
- **LLM**: 조치와 결과를 인과로 묶기 (핵심, 임베딩으로는 원천적으로 불가)

임베딩은 "비슷한가"를 보고 LLM은 "이어지는가"를 본다.
시간차 두고 나온 결과 문장("지난달 캐싱 작업 이후로 지표 좋아짐")은 조치 문장과
어휘가 겹치지 않으므로 유사도로 못 묶는다. 이 구분을 흐리지 말 것.

### 2.4 스코프 확장 금지 목록
아래는 논의를 거쳐 **의도적으로 배제**한 것들이다. 다시 제안하지 말 것.

> **2026-09-18 — "연차 직접 입력" 한 줄만 해제됨.** Figma "00 · 온보딩"(`268:5731`)
> 재설계가 연차 세그먼트를 없애고 **회사·기간을 받아 연차를 계산**하는 3/4 화면을
> 들여왔고, 사용자가 그 디자인을 보고 "전부 구현"을 명시적으로 지시했다.
> 저장되는 값은 **여전히 4개 구간**이다(서버가 `src/career_span.py`로 계산) — 사람에게
> 묻는 방식만 바뀐 것이라 이 값을 읽는 쪽(경력기술서 제목 등)은 영향이 없다.
> **나머지 4개는 그대로 유효하다.**

| 배제 항목 | 사유 |
|---|---|
| 채용공고 URL 크롤링 | 붙여넣기로 충분. 사이트별 파싱에 이틀 녹음 |
| Jira 연동 | 회사 계정 연동을 유저가 거부 + 관리자 승인 불가 |
| 노션 쓰기(write) | 읽기 + 클립보드 복사로 충분. OAuth 스코프 확대 부담 |
| 폴더 CRUD 고도화 | 프로젝트 생성/이름변경 최소 기능만 |
| ~~연차 직접 입력~~ | **2026-09-18 해제** — 아래 주석 참고 |

---

## 3. 데이터 모델 (신규 — 이전 버전에 없던 층)

```sql
projects(id, name, started_at, ended_at, is_current)
cards(id, project_id FK, raw_text, refined_sentence,
      skill_tags JSON, confidence, created_at)
```

**프로젝트는 사람이 만들고, 그 안의 작업 묶음은 AI가 만든다.**

SI처럼 고객사를 옮겨 다니는 경우 A은행의 "결제 API 개선"과 B카드의 "결제 API 개선"은
내용이 거의 같아도 경력기술서에 별개로 들어가야 한다. 섞이면 경력이 반으로 줄어든다.
내용 유사도로는 이 구분이 원천적으로 불가능하므로 **바깥 상자(프로젝트)는 유저가 지정**한다.

`is_current`로 현재 상주 프로젝트를 표시하고 새 카드는 자동으로 거기 들어간다.
유저는 프로젝트가 바뀌는 날에만 손댄다(SI 기준 연 1~3회). 2.1 원칙을 지키는 설계다.

AI가 만드는 작업 묶음은 **DB에 저장하지 않는다.** 초안 생성 시점에 LLM이 그때그때 묶는다.
저장하면 관리 UI가 필요해지고 그 순간 2.1이 깨진다.

---

## 3.5 디자인 원본

```
https://www.figma.com/design/MbQqcE5hGWI5KXwT3OtvIb/Connecting-Dots
  node-id=10-2   기획 정리 (화면 흐름, 타임라인, 페르소나)
  node-id=41-10  화면 목업 (2.0 온보딩 ~ 4.2 빌더)
```

읽으려면 **Figma MCP 연결 + 편집자 권한**이 둘 다 필요하다. 뷰어 권한으로는 거부된다.
접근 방법과 화면 대응표는 `tasks/track-c-frontend.md` 3장.

> **2026-09-16 개정 — 우선순위 뒤집힘.** 발표 데모를 앞두고 사용자가 명시적으로
> "지금은 이 문서보다 Figma를 우선으로 보고, 다른 부분이 있으면 이 문서를 Figma
> 기준으로 고쳐라"고 지시함. 그 이전까지는 "Figma와 문서가 충돌하면 문서가 우선"
> 이었으나, **이 지시 이후로는 반대로 Figma가 단일 진실 공급원이고 이 문서가
> Figma를 따라간다.** 실제로 이 개정 이후 세션에서 Figma 재검토로 다음이 바뀜:
> - 하단 탭 4개(입력/스택/경력기술서/프로젝트) → Figma와 동일하게 2개(일지 기록/
>   커리어 스택)로 축소, `/projects` 라우트 삭제(프로젝트 전환/생성은
>   `<ProjectSwitcher />`로 `/`·`/stack`에 인라인 통합 — 89:525 대응)
> - "기존 경력기술서 붙여넣기 → Before/After 대조"(90:612/90:640), STAR 항목
>   개별 필드 직접 수정(89:419), 오프라인 배너(41:762), 초안 생성 실패 전용
>   화면(41:737) 등 미구현 화면들을 신규 구현
> - 온보딩 "나중에 설정하기" 스킵 경로 추가(AuthGate 강제 리다이렉트도 세션
>   플래그로 같이 완화)
>
> **단, 2.4 배제 목록(아래) 5개는 이 우선순위 뒤집힘과 무관하게 그대로 유지된다**
> — Figma에 해당 화면이 그려져 있더라도, 배제 사유가 "디자인 의견 차이"가 아니라
> "회사 계정 연동 거부", "OAuth 키 미발급", "사이트별 파싱 시간 비용" 같은 실제
> 인프라/조직 제약이라 문서 우선순위를 뒤집는다고 해결되지 않는다. Figma에서
> 이 5개와 충돌하는 화면을 발견하면 구현하지 말고 사용자에게 먼저 확인할 것.
>
> Figma 코멘트 핀의 논의 내용은 어떤 도구로도 읽을 수 없으므로(Plugin API에 comment 접근
> 자체가 없음), 거기서 나온 결정 중 이 개정 이전 것들은 이 문서로 옮겨져 있다.

---

## 4. 아키텍처

```
[브라우저]  Next.js — 화면, 마이크, PWA, 웹푸시
                ↓
[OCI VM]    nginx (TLS 종단, 리버스 프록시)
              ├─ /       → Next.js  (localhost:3000)
              └─ /api/   → FastAPI  (localhost:8000)
                             └─ SQLite + Chroma (로컬 디스크)
```

Streamlit을 걷어낸 이유는 iframe 제약이다. `components.html`이 만드는 샌드박스 iframe
안에서는 마이크 권한, 서비스워커 scope(`/app/static/` 하위로 제한), manifest 주입이
전부 정상 동작하지 않는다. 기존 `install_button.py` 주석이 이 실패를 스스로 기록하고 있다.

OCI 단일 VM에 상주 프로세스로 올리므로 **콜드 스타트와 디스크 유실 문제가 해소**된다.
`docs/03-risk-fallback.md`의 관련 조항은 이번 개정에서 삭제했다.

---

## 5. 트랙 구성 (worktree/브랜치)

> **진행 현황 (2026-09-14 갱신)**: 아래 표는 9/13 피벗 시점 기준 계획이다. 실제로는
> 같은 날 세션에서 P0 세 개(저장소/`build_resume()`/프로젝트 층+FastAPI)를 전부
> 끝냈고, 9/14엔 P1 라우터(JD 매칭/노션 REST+MCP), Track E/F 이식(마이크/PWA/웹푸시),
> 온보딩 프로필(`/onboarding`), Figma 디자인 반영(핵심 4화면)까지 마쳤다. 남은 건
> **Track D(OCI 배포)뿐**이며, 이건 실제 VM/도메인 접속 정보가 있어야 진행 가능하다.
> 실기기(iPhone/Android) 검증은 여전히 미완료 — CLAUDE.md 8장 참고. 세부 현황은 각
> `tasks/track-*.md`의 체크리스트가 최신 진실이다.
>
> **추가 갱신 (2026-09-16)**: 발표 데모 준비 중 Figma 전면 재검토(3.5장 "우선순위
> 뒤집힘" 참고)로 다음을 새로 구현/수정함 — 기존 경력기술서 붙여넣기 → Before/After
> 대조(`/resume/enhance` 신규), STAR 항목 개별 필드 직접 수정(재생성해도 유지),
> 초안 생성 실패 전용 화면, 오프라인 배너, 하단 탭 2개로 축소(`/projects` 삭제),
> 온보딩 스킵 링크. 또한 발표 체감 속도 문제 2건을 해결함 — (1) 매일 쓰는 메모
> 정리 경로를 Sonnet에서 Haiku로 분리(`LLM_MODEL_FAST`), (2) 태그 캐노니컬라이제이션
> 임베딩 호출을 query/upsert 중복 없이 최대 1회로 줄이고, `OLLAMA_BASE_URL`을
> `localhost`에서 `127.0.0.1`로 바꿔 Windows의 IPv6 우선 시도 지연(호출당 2~3초)을
> 제거함 — 카드 생성 전체가 체감상 수 초대로 단축됨.
>
> **추가 갱신 (2026-09-18)**: Figma 전수 재조사(캔버스 `10:2`, 화면 39개)로 남아
> 있던 미구현 화면을 구현함 — ①**4.2.1 범위 선택**(`89:161`, "그동안 이런 걸
> 하셨어요"): `/resume`이 현재 프로젝트 하나에 고정돼 있어 "마스터 경력기술서"
> 버튼이 실제로는 마스터가 아니었다. `POST /api/resume`가 `project_ids` +
> `include_unassigned`를 받도록 넓히고(프로젝트 경계는 유지 — 5장 참고) 결과에
> 프로젝트 헤드(`41:254`)를 그린다. 범위가 여러 개인 문서의 초안은 프로젝트에
> 귀속되지 않아 `master_resume_drafts`(유저당 1개)에 저장한다. ②**4.1-b 기록
> 상세**(`89:479`, `/stack/<id>` 신규): 같은 주제(대표 태그+프로젝트)의 기록을
> 시간순으로 보여주고 거기에 바로 이어 쓴다. 이 묶음도 저장하지 않는다(3장).
> ③`/`의 이어 쓰기 배너(`41:119`)·타깃 트랙 칩(`41:116`)·노션 링크(`41:128`),
> 복사 완료 토스트(`41:880`), ④설정의 백업 내보내기/불러오기(`41:333`/`41:338`,
> `GET /api/backup` + `POST /api/backup/import` 신규 — 불러오기는 LLM을 타지 않고
> **덧붙이기만** 한다).
>
> 아직 남은 미구현 화면은 전부 **사용자가 이전 세션에서 보류를 확정한 것들**이다 —
> 2.1 이력서 파일 업로드(`89:6`), 노션 OAuth 3화면(`41:452`/`41:486`/`41:832`, client_id
> 미발급). 다시 제안하기 전에 사용자에게 확인할 것.
> (~~직무 전환·목표 직무 번역 5화면~~은 **2026-09-18 해제됨** — 아래 "변환 결과 재설계" 참고.)
>
> **온보딩 전면 재설계 (2026-09-18, Figma `268:5731` "00 · 온보딩" 섹션)**: 그동안
> 캔버스 `10:2`에만 있던 온보딩이 새 섹션으로 다시 그려졌고, 사용자 지시로 전부
> 구현했다. 한 화면(직군 6칩 → 세부직무 → 연차 4세그먼트 → 알림)이 **4단계**로 나뉜다 —
> ①현재 직무(단일, 검색+직군 세그먼트+칩 클라우드) ②**목표 직무(다중)** — 그동안
> 보류돼 있던 항목 ③**회사·기간 → 연차 자동 계산** — 2.4 배제 목록 한 줄이 이걸로
> 해제됐다(2.4장 주석) ④퇴근 알림(시각 칩 + 주말 제외 토글).
>
> 같이 들어온 것: **브랜드 강조색 `#ff5d00`** — 그전까지 이 앱엔 강조색이 없었고 주요
> 버튼이 전부 흰색이었다. 앱 전체의 주요 버튼 23개를 오렌지로 바꿨다(장식용 점·막대·
> 체크는 그대로). 토큰은 `web/app/globals.css`의 `--accent`이고 `bg-accent` /
> `text-accent-foreground`로 쓴다.
>
> 직무 목록(`web/lib/jobTaxonomy.ts`)은 Figma에 일부만 그려져 있어 **나머지를 우리가
> 구성했다** — 원티드 공식 분류표가 아니다. 확정된 분류가 생기면 그 파일만 갈아끼우면
> 된다. `web/`엔 테스트 러너가 없으니 직무 이름 유일성은 사람이 지킬 것(파일 주석 참고).
>
> **변환 결과 재설계 (2026-09-18, Figma `286:7123` "02 · 변환 결과" 섹션)**: 결과 출력
> 모달이 새 섹션으로 다시 그려졌고, 사용자 지시로 화면 6개를 전부 구현했다.
> - **3.1 결과 출력 모달** — 관점 라벨("퍼포먼스 마케터 관점")·헤드라인 20px·"문장
>   고치기"(연필)·[다시 만들기]/[복사하기]. 기존 시트에 있던 ✕ 버튼과 스킬 태그 칩은
>   재설계에서 빠졌다(태그는 `/stack`에서 계속 보이고 고칠 수 있다).
> - **3.1-d 결과 문장 직접 수정** — "직접 고친 문장은 다시 변환해도 유지돼요" +
>   "AI 문장으로 되돌리기". 둘을 동시에 지키려고 `cards`에 `ai_sentence` /
>   `sentence_edited` 두 컬럼을 추가했다(마이그레이션 멱등). `/refine`("다시 만들기")은
>   `sentence_edited`인 카드의 문장을 덮어쓰지 않고 `ai_sentence`만 갱신한다.
> - **3.1-a 복사 완료 토스트** — 기존 `<Toast />`(41:880) 재사용.
> - **3.1-q 변환 전 추가 질문** — 2.2가 정한 "되묻기" 경로(`POST /api/cards/metric-question`
>   신규). **항상 뜨지 않는다**: 수치가 정말 빠진 기록에만 질문이 오고 나머지는 빈
>   문자열이 와서 화면을 건너뛴다(2.1). 답은 `POST /api/cards`의 `metric_answer`로 가고
>   서버가 `raw_text` 뒤에 한 줄로 붙인다 — 유저가 타이핑한 값만 들어가므로 2.2 위반이 아니다.
> - **3.1-b / 3.1-c 직무 전환 번역** — 2.4 배제 목록이 아니라 "보류 확정" 목록에 있던
>   항목이고, 사용자가 이 섹션을 보고 "6개 전부 구현"을 지시해서 해제했다
>   (`POST /api/cards/{id}/translate` 신규). 온보딩 2/4의 `target_jobs`를 쓰고, 결과는
>   **저장하지 않는다**(3장과 같은 판단). 접점이 없으면 억지로 붙이지 않고
>   `related: false`로 3.1-c 화면이 된다.
>
> 관점 전환(3.1 ↔ 3.1-b/3.1-c)은 **관점 라벨 자체를 누르는 것**으로 붙였다. 목업엔
> 전환 컨트롤이 그려져 있지 않은데, 매일 쓰는 경로에 버튼을 더할 수는 없어서(2.1)
> 기본값을 현재 직무 관점으로 두고 목표 직무가 등록된 사람에게만 라벨 옆에 `›`를 붙였다.
>
> 이 섹션은 색도 바뀌었다 — 중성 회색(`#1a1a1a`/`#242424`/`#f2f2f2`)이 아니라 따뜻한
> 톤(`#1b1b1b`/`#2a2724`/`#ede9e2`/`#918c84`)이다. 사용자가 적용 범위를 **이 섹션
> 화면만**으로 확정해서 `globals.css` 전역 토큰은 그대로 두고 `web/components/resultTheme.ts`에
> 모아 뒀다. 홈/경력기술서는 여전히 기존 회색이다(커리어 스택은 아래 재설계로 같은
> 따뜻한 톤이 됐다).
>
> **커리어 스택 재설계 (2026-09-18, Figma "03 · 커리어 스택 · Tab B")**: 사용자 지시로
> 화면 9개를 구현했다. **사진 첨부는 이 레포에 없던 신규 기능이고, 사용자가 "사진까지
> 전부 구현"으로 확정했다.**
> - **4.1-h `/stack` 재설계** — 스택 헤드("기록 N개를 역량 M개로 나눴어요") · 비중
>   스트립 · 분류 헤더 + [수정] · 역량 리스트(→ 역량 상세) · + 역량 추가 · 역량 공백
>   경고 · 내 경력기술서 진입 · 마스터 초안 버튼. **기존 카드 리스트는 지우지 않았다**
>   — 4.1-e가 같은 화면에 카드 리스트를 그리고 있어서 4.1-h 목업에 없는 게 "제거"인지
>   "그 부분만 안 그린 것"인지 확정할 수 없었다. 스트릭·액션시트·그룹 보기가 거기
>   달려 있어 지우는 쪽이 손실이 크다고 판단함. **사용자 확인 필요.**
> - **4.1-j / 4.1-k 역량 상세** (`/stack/skill/[tag]` 신규) — 역량별 기록 목록 +
>   "이 역량으로 문장 만들기". 기록이 없으면 4.1-k("이런 일을 하셨을 때 쌓여요" 예시).
>   문장은 저장하지 않는다(3장). `POST /api/resume`의 `skill_tag` 신규.
> - **4.1-i 분류 수정** (`/stack/classify` 신규) — 태그 없는 기록에 임베딩으로 역량
>   2개를 추천(`GET /api/cards/tag-suggestions` 신규). **LLM이 아니라 임베딩**이고
>   (2.3), 후보는 **이미 있는 역량**뿐이며, 확정은 사람이 누를 때만 저장된다.
> - **4.1-d 빈 상태** — 로고 + 백업 불러오기 진입 + 기록하러 가기.
> - **4.1-b 기록 상세** — 첨부 사진 스트립 + 타임라인의 "· 사진 N".
> - 4.1-a/4.1-c/4.1-e는 기존 구현을 유지했다(재설계에서 구조가 안 바뀜).
>
> **사진 첨부 설계**: 이미지는 DB BLOB이 아니라 디스크(`PHOTO_DIR`)에 두고 DB엔 메타만
> 남긴다 — BLOB이면 백업 내보내기가 사진까지 메모리에 올려서 OCI AMD Micro(RAM 1GB)에
> 부담이다. 서버에 Pillow를 안 올리는 대신 프론트가 canvas로 긴 변 1600px JPEG로
> 줄여서 보낸다(`web/lib/imageResize.ts`). 카드 삭제 시 사진 행과 파일도 같이 지운다
> (4.1-c "되돌릴 수 없어요").
>
> **목업에 없어서 우리가 채운 것 두 가지** (둘 다 사용자가 그 조건을 알고 승인함):
> ①**사진을 붙이는 UI** — 이 섹션엔 보는 화면만 있어서, 홈 입력창에 첨부 버튼을,
> 기록 상세 사진 스트립 끝에 추가 타일을 넣었다. ②**직군별 "자주 묻는 역량" 목록과
> 역량별 예시 문장**(`web/lib/coreCompetencies.ts`) — `jobTaxonomy.ts`와 같은 성격으로
> **우리가 쓴 것이고 원티드 공식 자료가 아니다**. 확정 자료가 생기면 그 파일만 갈아끼우면
> 된다. 예시 문장은 전부 "~했을 때" 형태라 경력기술서 문장으로 오인될 수 없게 했다(2.2).
>
> **Figma 노드 ID 주의**: 이 섹션은 작업 도중 캔버스가 재생성되면서 노드 ID가 통째로
> 바뀌었다(`294:*` → `301:*`, "02 · 변환 결과"도 `286:*` → `299:12341`). 이 문서와
> 코드 주석의 ID는 **작성 시점 값**이라 지금은 안 맞을 수 있다 — 화면 이름(4.1-h 등)으로
> 찾는 편이 안전하다.
>
> **홈·기록 재설계 + 저장본 (2026-09-18, Figma 캔버스 `100:692` 전수 대조)**: 디자인
> 46화면을 코드와 대조해 미구현 6개를 찾았고, 그중 5개를 구현했다(사용자가 4.1-l
> 하나만 제외하도록 지시).
> - **3.0 홈 전면 재설계** (`303:15859`) — 예전 홈(입력창 + 역량 버블 + 오늘 목록)이
>   **대시보드 + 진입점**으로 바뀌었다. 워드마크, "오늘은 / 어떤 일을 하셨나요?",
>   오늘 배너, 누적 요약 3개, 최근 기록 2줄, 하단 알약 입력 바. **앱에서 유일하게
>   밝은 영역이 여기 생겼다**(크림색 패널) — 토큰은 `stackTheme`의 `light1`/`light2`/
>   `onLight`/`lightRule`이고 배경은 순검정(`homeBg`)이다. 설정·프로젝트 스위처는
>   이 화면에서 빠졌고 `/stack`에 남아 있다.
> - **3.3 기록하기** (`/record` 신규) — 실제로 쓰는 자리. **변환·되묻기(3.1-q)·결과
>   시트(3.1/3.1-b/3.1-c/3.1-n)·문장 수정(3.1-d)·사진 첨부가 전부 이리로 옮겨왔다.**
>   홈에 남겨두면 두 화면에 같은 흐름이 복제돼 한쪽만 고쳐지는 버그가 난다.
>   `?topic=`으로 역량 맥락을 받고 [해제]로 뗄 수 있다.
> - **3.2 내 기록** (`/records` 신규) — 필터 칩 4개(전체/변환 전/미분류/수치 없음).
>   셋 다 **이미 받아온 카드로만** 판정해서 칩을 눌러도 서버를 다시 부르지 않는다(2.1).
> - **3.1-n 결과 · 수치 없음** — 결과 시트에 주황 테두리 안내 + [지금 채우기].
>   `POST /api/cards/{id}/metric-answer` 신규. 3.1-q에서 **건너뛴 질문이 있으면 그
>   질문을 그대로** 다시 보여주고, 없으면 일반 문구로 간다 — 묻지도 않은 질문을
>   지어내지 않는다. "수치 없음" 판정은 정규식(`/\d/`)이라 LLM을 부르지 않는다.
> - **4.3 내 경력기술서 저장본** (`/resume/saved` 신규) — `saved_resumes` 테이블 신규.
>   **작업 중 초안(`resume_drafts`, 프로젝트당 1개 덮어쓰기)과 별개 저장소다** —
>   공고마다 다르게 쓴 버전을 이름 달아 나란히 둔다. 항목 수/기록 수/공고 기반은
>   저장 시점에 실제로 센 값을 받아 보관한다(본문에서 역산하면 저장 당시와 달라진다).
>   `/resume`에 [보관] 버튼, `/stack`의 "내 경력기술서"가 이 목록으로 간다.
>
> **하지 않은 것**: 4.1-l 역량 선정 근거(사용자 제외 지시). 노션 3화면(client_id
> 미발급으로 보류 확정)과 4.2-j 공고 URL 입력(2.4 배제 목록)도 그대로 남아 있다.
>
> 하단 탭 라벨이 "일지 기록" → "기록"으로 줄었고, `/record`와 `/stack/classify`는
> 탭을 숨긴다(목업에 없고 주 액션 버튼과 자리를 다툰다).
>
> **4.1-l 역량 선정 근거 (2026-09-18 추가 구현)**: 앞서 사용자가 보류시켰던 화면인데,
> 섹션이 다시 재편되면서(`307:17003`, 이름은 "04 · 경력기술서"인데 내용은 커리어 스택
> 4.1-* 화면들이다) 4.1-j에 **"왜 이 역량인가요 ›"** 링크가 새로 생겼고, 그 링크가
> 갈 곳이 4.1-l이라 함께 구현했다(`/stack/skill/[tag]/why`).
>
> **출처 문구는 뺐다.** 목업은 직군 기준 카드 아래에 "원티드 직군별 채용 데이터 기준 ·
> 9월 갱신"이라고 적지만 우리에겐 그 데이터가 없다 — 없는 출처를 적는 건 2.2 위반이고
> 사용자가 "출처 문구는 빼고"로 확정했다. 칩 목록은 `lib/coreCompetencies.ts`
> (우리가 구성한 목록)에서 온다.
>
> **키워드 칩은 새로 계산하지 않는다.** 목업의 "매칭 단어"는 `parse_note()`가 그
> 기록에서 실제로 뽑은 `skill_tags`를 그대로 쓴다 — 4.1-h가 "쓰신 기록의 키워드를 읽고
> AI가 먼저 나눠뒀어요"라고 말하는 그 키워드가 이것이고, 분류의 실제 근거도 이 값이다.
> 따로 매칭 로직을 만들면 **실제 분류 근거가 아닌 걸 근거라고 보여주는** 셈이 된다.
>
> 이 섹션의 나머지 차이는 간격 미세 조정뿐이라(역량 리스트 항목 64→68px, 액션 시트
> 메뉴 60→56px 등) 쫓지 않았다.
>
> **노션 연동 전면 재작성 (2026-09-18) ★ 데이터 유출 수정**: 사용자가 "허용하지 않은
> 페이지까지 다 가져온다"고 신고해 원인을 찾아 걷어냈다.
>
> **무엇이 잘못됐었나**: `fetch_notion_entries()`가 `/v1/search`를 **필터 없이** 부르고
> 페이지네이션으로 끝까지 긁은 뒤, 그 본문을 전부 `run_pipeline_batch()`로 LLM에 태워
> 카드로 저장했다(`POST /api/notion/sync`). 노션은 부모 페이지를 통합에 공유하면
> **하위 트리 전체**에 권한을 주기 때문에, 부모 하나만 연결했다고 생각해도 통합은 그
> 트리를 전부 본다. 결과적으로 사용자가 의도하지 않은 문서가 우리 서버를 거쳐
> Anthropic까지 나갔다. 상한 50개도 정렬 없이 앞에서부터 잘라서 **어떤 50개가 나갈지
> 예측조차 불가능**했다.
>
> **지금 구조** (Figma "01 · 기록 · Tab A"의 3.0-b "노션 페이지 선택" 그대로):
> `POST /api/notion/pages`가 **제목과 수정 시각만** 주고 → 사용자가 목록에서 **하나를
> 고르고** → `POST /api/notion/pages/{id}/content`가 그 하나의 본문만 가져와 **입력창에
> 채운다** → 사용자가 읽고 고친 뒤 [문장으로 바꾸기]를 눌러야 변환된다. 두 엔드포인트
> 모두 **LLM을 부르지 않고 카드를 만들지 않는다.** 무엇이 외부로 나가는지를 사용자가
> 매번 눈으로 보고 정한다는 게 요점이다 — **대량 가져오기 경로를 다시 만들지 말 것.**
>
> **같이 지운 것**: MCP 경로(`fetch_notion_entries_via_mcp()` 등 약 150줄)도 제거했다.
> 같은 무필터 검색을 쓰면서 오직 sync만 호출하던 코드라 sync가 사라지면 어디서도 안
> 불렸고, 실제 MCP 서버로 end-to-end 검증된 적도 없다(9/14 노트가 그렇게 적고 있다).
> git 히스토리에 남아 있다.
>
> **토큰은 서버에 저장하지 않는다.** 노션 통합 토큰은 그 워크스페이스를 읽을 수 있는
> 서드파티 자격증명이라 SQLite에 평문으로 눕히지 않았다 — 요청마다 받고, 프론트가
> `sessionStorage`에만 들고 있다(`web/lib/notionToken.ts`). Figma의 "처음 한 번만
> 연결하면 돼요"보다 짧지만(탭을 닫으면 사라짐) 자격증명을 오래 들고 있지 않는 쪽을
> 택했다. **바꾸려면 사용자 확인이 필요하다.**
>
> **3.0-a 노션 인증(OAuth 웹뷰)은 여전히 미구현이다** — client_id가 발급되지 않아서
> 진짜 OAuth 화면은 만들 수 없다(그 화면은 notion.so가 그리는 것이라 애초에 우리가
> 만들 수 있는 게 아니다). 대신 통합 토큰을 직접 넣는 단계로 대체했다.
>
> 다만 **3.0-a에서 건져올 수 있는 게 하나 있었다**: 권한 카드의 두 줄
> ("최근 수정한 페이지 목록 조회", "선택한 페이지의 본문 읽기")이 우리가 실제로 부르는
> API 두 개와 정확히 일치한다. 그래서 그 두 줄을 토큰 입력 단계에 그대로 옮겨 적었다 —
> 무엇을 읽는지 밝히는 건 OAuth 없이도 할 수 있고, 유출 신고를 받은 기능이라 더더욱
> 필요하다. 이 목록에 없는 동작은 하지 않는다(쓰기 권한 미사용).

> **01 섹션 재검토 (2026-09-18)**: 3.1 / 3.3 / 3.0-i가 **같은 화면(`/record`)의 세
> 상태**라는 걸 뒤늦게 확인했다. 처음엔 3.3만 보고 만들어서 나머지 둘에만 있던 블록을
> 빠뜨렸다 — **홈을 대시보드로 재설계하면서 갈 곳을 잃은 것들이다**:
> - **타깃 트랙 칩**(3.0-i) — 어떤 직무 기준으로 다듬어지는지. 옛 홈에 있었다.
> - **이어 쓰기 배너**(3.0-i) — "어제 쓰던 [X]에 이어 쓰기". 옛 홈에 있었고,
>   제품의 핵심인 "시간차 누적"을 유도하는 유일한 장치였다. 없어진 줄도 모르고 있었다.
> - **설정 진입 ⚙**(3.1 우상단) — 새 홈엔 없어서 기록 탭 쪽 입구가 아예 사라져 있었다.
>
> 셋 다 `/record`로 옮겨 복구했다. 화면 하나를 목업 하나만 보고 만들면 이렇게 빠진다 —
> **같은 화면의 상태 변형들을 먼저 모아 보고** 합집합으로 구현할 것.

| 트랙 | 브랜치 | 범위 | 상태 |
|---|---|---|---|
| A | `track-a-prompt` | `parse_note()` + `build_resume()` | ✅ 완료 |
| B | `track-b-agent` | 저장소 + 프로젝트 층 + Chroma + Notion(REST+MCP) + FastAPI + 온보딩 프로필 | ✅ 완료 |
| C | `track-c-frontend` | Next.js — 화면 5개(+온보딩) + 마이크 + PWA + 웹푸시 + Figma 반영 | ✅ 완료, **실기기 검증 대기** |
| D | `track-d-deploy` | OCI + nginx (Streamlit Cloud 폐기) | ⬜ 미착수 — VM 접속 정보 필요 |
| E | `track-e-push` | 웹푸시 — 이식 완료 | ✅ 이식 완료, **실기기 수신 확인 대기** |
| F | `track-f-voice` | 음성 입력 — 이식 완료 | ✅ 이식 완료, **iOS 실기기 검증 대기** |

### 트랙 간 계약 (먼저 고정할 시그니처)

```python
# src/storage/db.py  — 신규
def save_card(project_id: int | None, parsed: ParsedEntry, created_at: str) -> int
def list_cards(project_id: int | None = None) -> list[Card]
def create_project(name: str, started_at: str) -> int
def list_projects() -> list[Project]
def set_current_project(project_id: int) -> None

# src/parsing/resume.py  — 신규, 제품의 핵심
@dataclass
class StarItem:
    title: str; period: str
    situation: str; task: str; action: str; result: str
    source_dates: list[str]      # 근거 추적용 — 반드시 반환할 것
    # 9/18 추가 — 마스터 경력기술서에서 어느 프로젝트에서 나온 항목인지.
    # build_resume()은 한 프로젝트 안에서만 묶으므로 이 둘을 채우지 않는다.
    # 호출부(build_career_doc)가 찍는다. 단일 프로젝트 초안에서는 None.
    project_id: int | None = None
    project_name: str | None = None

def build_resume(cards: list[Card], jd_text: str | None = None) -> list[StarItem]

# src/agent/pipeline.py — 9/18 범위 확장 (Figma 4.2.1 "범위 선택")
def build_career_doc(user_id: int, project_id: int | None = None, jd_text: str | None = None,
                     *, project_ids: list[int] | None = None,
                     include_unassigned: bool = False) -> list[StarItem]
```

`build_career_doc()`은 **프로젝트마다 따로 `build_resume()`을 부른다.** 여러 프로젝트를
한 번에 넣어 묶으면 3장이 말하는 "A은행 결제 API ≠ B카드 결제 API"가 깨진다 — 경력이
반으로 줄어든다. 이 분리를 없애지 말 것.

`source_dates`는 생략 불가다. 어느 카드에서 나온 문장인지 추적할 수 있어야
2.2 원칙(환각 금지) 위반을 검증할 수 있고, 유저에게 근거를 보여줄 수 있다.

**프론트/백 분리 이후 진짜 계약은 HTTP다.** `docs/05-api-contract.md`를 먼저 고정하고
Track C는 그 문서만 보고 작업한다.

---

## 6. 우선순위 (일정이 밀릴 경우 이 순서로 지킨다)

### P0 — 없으면 제품이 성립하지 않음
1. **저장소** — 현재 `st.session_state`뿐이라 새로고침 시 전부 소실. 누적이 없으면 제품이 없다
2. **`build_resume()`** — 여러 카드를 STAR로 묶는 기능. 현재 레포에 0줄
3. **프로젝트 층** — 테이블 + 스위처 UI

### P1 — 데모 임팩트
4. JD 붙여넣기 → 맞춤 재구성
5. 시드 데이터 (6.1 참조)

### P2 — 여유 시
6. 온보딩 (현재 직무 / 목표 직무 / 연차)
7. 숫자 되묻기 UI
8. 임베딩 자동 배정

### 6.1 시드 데이터 요구사항 (P1이지만 실질 필수)
`data/seed_cards.json`에 **프로젝트 2개 × 각각 조치/결과 페어 1쌍 이상**을 넣는다.

```
[A은행] 02.14 "결제 API 느려서 레디스 캐시 붙임"
        03.02 "레디스 붙인 뒤 결제 오류율 0.8% → 0.3%"   ← 시간차 필수
```

현재 목업 카드가 전부 02.13~02.18에 몰려 있어 **핵심 기능을 시연할 수 없다.**
시간차 페어가 없으면 발표에서 보여줄 장면이 사라진다.

---

## 7. 일정 (2026-09-13 기준, 마감 9/19 오후)

| 날짜 | 작업 | 게이트 |
|---|---|---|
| 9/13 (일) | **실기기 음성 테스트** + SQLite 저장소 | |
| 9/14 (월) | `build_resume()` STAR 생성 | |
| 9/15 (화) | 프로젝트 층 + FastAPI 래핑 | **P0 완료 확인** |
| 9/16 (수) | Next.js 프론트 | |
| 9/17 (목) | OCI + nginx 배포 | **배포 성공 확인** |
| 9/18 (금) | JD 매칭 + 시드 데이터 | |
| 9/19 (토) 오전 | 리허설 | |

**9/15 화요일이 분기점이다.** 이 시점에 P0 세 개가 안 끝났으면 프론트 전환을 중단하고
Streamlit으로 마감한다. 화면은 포기해도 제품은 남지만, 백엔드가 비면 보여줄 것이 없다.

배포는 마지막에 몰지 않는다. 9/16부터 매일 한 번씩 연습 배포할 것.

---

## 8. 선행 검증 (다른 작업보다 먼저 — 9/13 중 완료)

**실기기 음성 입력 테스트.**

```
1. 터널 또는 배포 주소를 실제 iPhone Safari로 접속
2. 음성 입력 버튼 → 마이크 권한 팝업이 뜨는가 / 인식되는가
3. Android Chrome으로 동일 테스트
```

| 결과 | 판단 |
|---|---|
| 양쪽 성공 | Web Speech API 유지 |
| iOS만 실패 | 브라우저 미지원. **녹음(`MediaRecorder`) + STT API로 전환** |
| 양쪽 실패 | iframe 문제 확정. Next.js 전환으로 해결됨 |

iOS 실패 시 UX가 "실시간 자막"에서 "녹음 버튼 하나"로 바뀐다.
화면 설계가 달라지므로 **디자인에 즉시 공유**할 것. 이 결과 전에 음성 화면을 확정하지 않는다.

---

## 9. 병렬 작업 규칙 (Orca / git worktree)

- 트랙 간 계약은 `src/` 하위 모듈의 **함수 시그니처와 docstring으로 먼저 고정**한 뒤 구현한다
- 프론트/백 분리 이후 API 계약은 `docs/05-api-contract.md`가 단일 진실 공급원이다.
  이 문서를 바꾸면 Track C에 즉시 알린다
- 병합 전 `tests/` 해당 트랙 테스트를 통과시킬 것
- 백엔드 공용 설정은 `src/config.py`와 `.env`, 프론트는 `.env.local`
- CORS 허용 origin은 하드코딩 금지, 환경변수로 관리

---

## 10. 에이전트 작업 체크리스트

- [ ] `docs/`와 해당 `tasks/track-*.md`를 읽었는가
- [ ] 2장의 **절대 원칙 4개**에 위배되지 않는가
- [ ] 매일 쓰는 경로에 유저 선택지를 추가하지 않았는가 (2.1)
- [ ] LLM 프롬프트에 "숫자를 추정하지 마라" 제약이 들어 있는가 (2.2)
- [ ] 2.4 금지 목록에 있는 기능을 다시 제안하고 있지 않은가
- [ ] 시그니처나 API 계약을 변경했다면 다른 트랙에 영향 없는지 확인했는가
- [ ] `.env.example`에 새 환경변수를 추가했는가 (실제 키는 커밋 금지)
- [ ] 리스크 구간에서 하루 이상 막히면 즉시 폴백으로 전환했는가

---

## 11. 이 개정에 따른 정리 대상

- [x] `CLAUDE.md` — 본 문서
- [x] `tasks/track-a-prompt-engine.md` — `build_resume()` 추가
- [x] `tasks/track-b-agent-pipeline.md` — 저장소·프로젝트 층 추가
- [x] `tasks/track-c-frontend.md` — Next.js로 재작성
- [x] `tasks/track-d-deploy.md` — OCI + nginx로 재작성
- [x] `docs/01-schedule.md` / `02-architecture.md` / `03-risk-fallback.md` — 재작성
- [x] `docs/05-api-contract.md` — 신규
- [x] `docs/06-migration.md` — 신규 (Track E/F 이식 절차)
- [x] `tasks/track-e-push-notifications.md` / `track-f-stt-voice-input.md`
      — 본문은 유지하고 상단에 `docs/06-migration.md` 참조 링크 추가함 (9/13)
- [x] `logs/` 신설 — 기존 `tasks/track-{a,b,c,d}.md`에 있던 트러블슈팅/회고 기록을
      개정으로 덮어쓰기 전에 `logs/track-{a,b,c,d}.md`로 이관해서 보존함 (9/13)
- [ ] `src/frontend/` — **아직 삭제하지 않음.** 파일 자체는 참고/폴백용으로 남겨두되,
      9/13~14 사이 게이트를 통과하고 Next.js가 실제로 기능적으로 완성돼 requirements.txt
      제거는 이미 진행함(아래 항목). 파일 자체의 최종 폐기는 OCI 배포 확정 후로 미룸 —
      iframe 우회 코드(`install_button.py` 등)는 이식하지 않는다
- [x] `manifest.json`, `README.md` — 제품명 갱신 (9/14). `web/app/manifest.ts`가
      "커리어 로그"로, `README.md`가 현재 아키텍처(Next.js+FastAPI+SQLite+Chroma) 기준으로
      전면 갱신됨
- [x] `requirements.txt` — streamlit/plotly/langchain 제거, fastapi/uvicorn 추가 (9/14).
      Next.js 전환이 실제로 완료·검증돼 제거 조건 충족으로 판단함
