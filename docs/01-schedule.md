# 일정 (2026-09-13 개정)

마감: **9/19(토) 오후**. 남은 기간 오늘 포함 7일.

---

## 9/13 (일) — 선행 검증 + 저장소

**최우선: 실기기 음성 입력 테스트.** 다른 작업보다 먼저, 5분.
결과가 Next.js 전환 여부와 음성 UX 설계를 동시에 가른다 (CLAUDE.md 8장).

- [ ] iPhone Safari / Android Chrome 음성 테스트
- [ ] `src/storage/db.py` — SQLite 스키마 + CRUD
- [ ] `data/seed_cards.json` 초안

## 9/14 (월) — 경력기술서 생성

제품의 핵심. 현재 레포에 0줄이다.

- [ ] `src/parsing/resume.py` — `build_resume()`
- [ ] 시간차 병합 프롬프트
- [ ] 환각 방지 테스트 (숫자 없는 입력에 숫자가 나오면 실패)

## 9/15 (화) — 프로젝트 층 + API 래핑  ⚠️ 게이트

- [ ] `projects` 테이블 + 자동 배정
- [ ] FastAPI 라우트 (`docs/05-api-contract.md` 기준)
- [ ] `docs/05-api-contract.md` 확정 — Track C가 이걸 보고 작업한다

> **분기점.** 이날 저녁까지 P0 세 개(저장소 / `build_resume()` / 프로젝트 층)가
> 안 끝났으면 **Next.js 전환을 중단하고 Streamlit으로 마감한다.**
> 화면은 포기해도 제품은 남지만, 백엔드가 비면 보여줄 것이 없다.

## 9/16 (수) — Next.js 프론트

- [ ] 화면 4개 (`/`, `/stack`, `/resume`, `/projects`)
- [ ] 마이크 / PWA / 푸시 이식 (`docs/06-migration.md`)
- [ ] **저녁: 연습 배포 1회**

## 9/17 (목) — OCI 배포  ⚠️ 게이트

- [ ] nginx + 인증서 + systemd
- [ ] 실기기 전체 루프 검증

> **분기점.** 이날 배포가 안 되면 로컬 데모로 발표 준비를 전환한다.
> 배포 실패보다 나쁜 건 배포를 붙잡다 발표 준비를 못 하는 것이다.

## 9/18 (금) — JD 매칭 + 시드

- [ ] JD 붙여넣기 → `build_resume(jd_text=...)`
- [ ] 시드 데이터 확정 — **시간차 페어 필수**
- [ ] 발표 자료 / 데모 시나리오 (디자인과 협업)

## 9/19 (토) 오전 — 리허설

- [ ] 실기기로 전체 시연 3회
- [ ] 두 systemd 서비스 상태 확인
- [ ] 네트워크 실패 대비 — 로컬 백업 데모 준비

---

## 이번 개정의 핵심 변경 4가지

1. **제품 방향 전환** — "온보딩 다이어리"에서 "커리어 로그"로.
   단건 변환이 아니라 시간차 누적이 핵심 가치
2. **스택 전환** — Streamlit → Next.js + FastAPI, Streamlit Cloud → OCI + nginx.
   사유는 음성 입력(iframe 제약)
3. **P0 재정의** — 저장소와 경력기술서 생성이 최우선.
   기존 계획에는 둘 다 없었고, 그 결과 선택 트랙(푸시/음성)이 먼저 완성됐다
4. **게이트 2개 도입** — 9/15(백엔드), 9/17(배포).
   각 시점에 포기 판단을 명시적으로 내린다
