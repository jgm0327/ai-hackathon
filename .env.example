# LLM
LLM_API_KEY=
LLM_MODEL=claude-sonnet-4-6

# Notion (REST API 우선)
NOTION_TOKEN=
# Notion MCP (선택, 타임박스 초과 시 미사용)
NOTION_MCP_SERVER_URL=

# Vector DB
CHROMA_PERSIST_DIR=./.chroma
MOCK_JD_DIR=data/mock_jds

# Tier 2 푸시 알림 (선택, Track E) — python -m py_vapid --gen 등으로 생성
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=mailto:example@example.com
