.env
.venv/
__pycache__/
*.pyc
.chroma/
.streamlit/secrets.toml
.DS_Store
# 로컬 폴백 전용 (Upstash 미설정 시에만 쓰임) — 구독자 기기 식별자가 담겨있어 커밋 금지
data/push_subscriptions.json
# 카드/프로젝트 SQLite DB — 실행 시 생성되는 실데이터, 커밋 금지 (data/seed_cards.json은 예시 데이터라 커밋함)
data/app.db
# 기록에 첨부한 사진 — 유저가 올린 실데이터라 커밋 금지 (9/18 신규)
data/photos/
# 배포 운영 노트 — 서버 경로/도메인/방화벽 등 이 환경에만 해당하는 내용이라 커밋하지 않는다
DEPLOY-OCI.local.md
# 실제 Pingbell 로그 수집 API 키가 채워진 파일 — deploy/fluent-bit.conf.example만 커밋한다
deploy/fluent-bit.conf
